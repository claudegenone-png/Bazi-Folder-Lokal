# sandbox1/

Reserved untuk **V4.5** development (next iteration).

V4.3 sudah promote ke production di `../v43/`.

Folder ini sengaja kosong. Mulai V4.5 dengan:
- Copy `../v43/engines/`, `../v43/templates/`, `../v43/assets/` sebagai baseline
- Iterate di sini tanpa mengganggu V3 (`../cache/`) atau V4.3 production (`../v43/`)
- Output testing → `sandbox1/_pdf_out/{date}/{Name}...-V4.5.pdf` (JANGAN ke `#result/`)

**Sisa PDF di `_pdf_out/2026-05-02/`** — sample test V4.3 sebelum promote. Boleh hapus kapan pun (saat PDF viewer ditutup).
